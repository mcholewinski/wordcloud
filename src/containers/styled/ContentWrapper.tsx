import styled from 'styled-components';

const ContentWrapper = styled.div`
  max-width: 1100px;
  padding: 0 16px;
  display: flex;
  flex-direction: column;
`;

ContentWrapper.displayName = 'AppContentWrapper';

export default ContentWrapper;
