import styled from 'styled-components';

const Label = styled.label`
  font-size: 24px;
  font-weight: bold;
  margin-bottom: 20px;
`;

Label.displayName = 'InputControlLabel';

export default Label;
