import styled from 'styled-components';

const TitleWrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  margin-bottom: 120px;
`;

TitleWrapper.displayName = 'HomeTitleWrapper';

export default TitleWrapper;
