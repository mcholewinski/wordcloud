import styled from 'styled-components';

const ButtonWrapper = styled.div`
  margin-top: 72px;
  display: flex;
  justify-content: center;
`;

ButtonWrapper.displayName = 'HomePlayButton';

export default ButtonWrapper;
